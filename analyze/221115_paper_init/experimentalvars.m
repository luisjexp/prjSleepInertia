function vars = experimentalvars()
    % simple way of getting list of experimental variables, consistently,
    % and avoiding getting wrong ones
    vars = {'cogtest','condition','band_ord','run','sbj','chan'};
end